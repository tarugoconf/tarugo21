<section class="maincontent-why" id="why">
  <header>
    <h2>{{ content.title | md(true) | safe }}</h2>
  </header>

  {{ content.intro | md | safe }}

  {# Community #}
  {% set cont = content.community %}
  <h3>{{ cont.title | md(true) | safe }}</h3>

  {{ cont.content | md | safe }}

  {# Play #}
  {% set cont = content.play %}
  <h3>{{ cont.title | md(true) | safe }}</h3>

  {{ cont.content | md | safe }}

  <video src="/img/objetivos/{{ content.goal }}.mp4" muted autoplay loop></video>

  {# <aside class="maincontent-goals">
    <h4>{{ cont.goals.title }}</h4>

    <dl>
      {% for goal in cont.goals.items %}
      <div class="maincontent-goal is-{{ goal.status }}" style="--percentage:{{ goal.percentage }}">
        <dt>
          {{ goal.title | md(true) | safe }}
        </dt>
        <dd>
          <div>
            {{ goal.description | md | safe }}
          </div>

          <div>
            {% if goal.status == "current" %}
            <img src="{{ '/img/objetivos/cavando.gif' | url }}" alt="Imagen del objetivo">
            {% else %}
            <img src="{{ goal.img | url }}" alt="Imagen del objetivo">
            {% endif %}
          </div>
        </dd>
      </div>
      {% endfor %}
    </dl>
  </aside> #}

  {# Purpose #}
  {% set cont = content.purpose %}
  <h3>{{ cont.title | md(true) | safe }}</h3>

  {{ cont.content | md | safe }}
</section>
